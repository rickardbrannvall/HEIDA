1. IMPLEMENT EASY OF UDERSTANDING OPERATIONS;

    use std::ops::{Add, AddAssign, Sub, SubAssign};
    impl Add for _ {
        type Output = Self;
        fn add(self, other: Self) -> Self {
            Self {x: self.x + other.x, y: self.y + other.y}
        }
        fn add(self, _rhs: f64) -> Self {
            Self {x: self.x + other.x, y: self.y + other.y}
        }
    }


2. PYTHON BINDINGS FOR ALL EXAMPLES AND FUNCTIONS THAT WILL HAVE BENEFITS/USE OUTSIDE RUST


3. PARALLELISE PGS